name: Update FF14 Character

on:
  schedule:
    - cron: "55 */3 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-ff14-feed:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install dependencies
        run: npm ci

      - name: Fetch FF14 character data
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        run: npx tsx scripts/update-ff14-feed.ts

      - name: Commit and push if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add public/data/ff14-character.json
          git diff --staged --quiet || git commit -m "chore: update FF14 character data $(date -u +%Y-%m-%d)" && git push
